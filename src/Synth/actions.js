export function handlePrev(scope) {

}

export function handleNext(scope) {
    
}

export function handleTrigger(instrument) {
    instrument.trigger();
}

export function handleSettings(scope) {
    
}

export function handleSave(scope) {
    
}